import styles from './Sidebar.module.scss';
function Sidebar() {
    return (
        <aside className={styles.wrapper}>
            <h1> sidebar</h1>
        </aside>
    );
}

export default Sidebar;
